# VovBlog 生产环境配置示例
# 复制此文件为 .env.production.local 并填入实际值
# ⚠️ 不要将此文件提交到 Git！

# ==========================================
# Cloudinary 图片存储配置
# ==========================================
# 从 https://cloudinary.com/console 获取

# 云名称（必需）
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=your_cloud_name

# API 密钥（必需）
CLOUDINARY_API_KEY=your_api_key

# API 密钥（必需，保密）
CLOUDINARY_API_SECRET=your_api_secret

# 上传预设（可选）
NEXT_PUBLIC_CLOUDINARY_UPLOAD_PRESET=vovblog_preset

# ==========================================
# 管理员身份认证（必需）
# ==========================================
# 强密码要求：
# - 最少 20 个字符
# - 包含大小写字母
# - 包含数字
# - 包含特殊字符
# 示例：Vov!Blog@2024$SecurePass123

ADMIN_PASSWORD=change_me_to_strong_password_min_20_chars

# ==========================================
# 网站配置
# ==========================================

# 主域名（用于子域名识别和 SEO）
NEXT_PUBLIC_SITE_URL=https://www.your-domain.com

# Google Analytics（可选）
# 从 https://analytics.google.com 获取 Measurement ID
NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# ==========================================
# Node.js 环境配置
# ==========================================

# 确保在生产环境中设置为 production
NODE_ENV=production

# ==========================================
# 可选：高级配置
# ==========================================

# 启用 debug 日志（仅在调试时使用，生产环境应关闭）
# DEBUG=*

# API 请求超时（毫秒，默认 30000）
# FETCH_TIMEOUT=30000

# Playwright 浏览器超时（毫秒，默认 30000）
# PLAYWRIGHT_TIMEOUT=30000

# ==========================================
# 不需要修改的默认值
# ==========================================
# 以下变量在部署时自动设置，无需修改：
# - PORT=3000（监听端口）
# - HOSTNAME=0.0.0.0（监听地址）
